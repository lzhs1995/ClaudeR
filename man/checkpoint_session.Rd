% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkpoints.R
\name{checkpoint_session}
\alias{checkpoint_session}
\title{Checkpoint the R session environment}
\usage{
checkpoint_session(
  label = NULL,
  envir = .GlobalEnv,
  dir = NULL,
  max_keep = 10L,
  max_gb = 4,
  force = FALSE
)
}
\arguments{
\item{label}{Optional short label recorded in the filename.}

\item{envir}{Environment to snapshot. Default `.GlobalEnv`.}

\item{dir}{Directory for checkpoint files. Default:
`~/.clauder_checkpoints/<session>/`.}

\item{max_keep}{Maximum checkpoints to retain in `dir` (oldest pruned).}

\item{max_gb}{Refuse to checkpoint if the objects total more than this
many gigabytes, unless `force = TRUE`.}

\item{force}{Set TRUE to checkpoint past the `max_gb` guard.}
}
\value{
The checkpoint file path, invisibly.
}
\description{
Saves every object in `envir` (default: the global environment) to a
timestamped .RData file so the session can be rolled back with
[restore_session()]. Old checkpoints beyond `max_keep` are pruned.
}
