% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/notebook.R
\name{export_log_as_notebook}
\alias{export_log_as_notebook}
\title{Export a ClaudeR session log as a Quarto lab notebook}
\usage{
export_log_as_notebook(
  log_path = NULL,
  output_path = NULL,
  title = NULL,
  include_errors = TRUE
)
}
\arguments{
\item{log_path}{Path to the ClaudeR session log. If NULL, uses the current
session's log file from settings.}

\item{output_path}{Path for the `.qmd`. Default: log path with a
`_notebook.qmd` suffix.}

\item{title}{Notebook title. Default is derived from the log filename.}

\item{include_errors}{If TRUE (default), errored blocks are kept as
non-evaluated chunks so the notebook records dead ends, not just wins.}
}
\value{
The output path, invisibly.
}
\description{
Parses a session log and writes a `.qmd` where each executed block becomes
an executable chunk, with the timestamp and agent recorded above it and a
`<!-- TODO: narration -->` marker for prose. Error blocks are included as
non-evaluated chunks with the error message, preserving the record of what
was tried. Rendering the notebook re-runs the session code, regenerating
outputs and plots.
}
